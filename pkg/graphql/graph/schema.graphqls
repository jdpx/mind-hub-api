# GraphQL schema example
#
# https://gqlgen.com/getting-started/

type Course {
  id: ID!
  title: String!
  sessions: [Session]
}

type Session {
  id: ID!
  title: String!
}

type Query {
  courses: [Course!]!
  sessions: [Session!]!
}
