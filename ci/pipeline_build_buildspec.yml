version: 0.2

phases:
  install:
    runtime-versions:
      golang: 1.13

  pre_build:
    commands:
      - make install
  build:
    commands:
      - make cb-lint
      - make test
      - make build-all-lambdas -B

artifacts:
  files:
    - "**/*"
  name: "mind_hub_api_build_artifact"
  base-directory: dist
